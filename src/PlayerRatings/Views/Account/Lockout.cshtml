@using PlayerRatings.Localization
@{
    ViewData["Title"] = LocString[nameof(LocalizationKey.LockedOut)];
}

<header>
    <h1 class="text-danger">@ViewData["Title"].</h1>
    <p class="text-danger">@LocString[nameof(LocalizationKey.LockedOutTryLater)].</p>
</header>
